# Changelog

本文档记录青卷的重要功能更新与行为变更。

## [0.5.0] - 2026-05-22

### 新增

- 新增基于 `Fluent Web Components` 的主界面，采用左侧导航栏、右侧功能区的桌面优先布局。
- 新增 Legado/阅读书源导入与管理能力，支持导入 `gedoor/legado` 兼容格式的自定义书源。
- 新增书源搜索分栏，默认使用全部已导入可搜索书源，并支持搜索结果一键加入书库。
- 新增书源书籍的章节缓存机制，阅读时自动缓存当前章节与后续 20 章。
- 新增桌面端与移动端统一打包脚本：`npm run build:release`、`npm run build:desktop`、`npm run build:mobile`。
- 新增移动端 Web / PWA 配置文件，支持生成可部署的 `release/mobile/` 静态产物。
- 后端生产模式支持托管前端静态资源，桌面端可直接通过本地 FastAPI 服务打开完整应用。

### 改进

- 放弃 Tauri 桌面壳，统一为 `Vue 3 + FastAPI` 本地服务架构，降低桌面构建复杂度。
- 书源加入书库改为轻量入库，章节正文按需下载与缓存，减少添加等待时间。
- 搜索流程改为模糊搜索并发执行，提高多个书源下的搜索可用性。
- 优化章节选择、书架、搜索、设置等页面的桌面端布局适配与控件对齐。
- 将项目版本统一提升为 `0.5.0`，同步更新前端包信息、后端服务版本与界面显示版本号。

### 修复

- 修复通过书源添加的书籍正文被当作 JSON 展示的问题。
- 修复通过书源加入书库后封面缺失的问题。
- 修复加入书库后章节目录只保留第一章的问题。
- 修复未缓存章节字数显示为 `0` 的问题，改为展示更准确的缓存状态。
- 修复搜索界面选择书源、结果展示与报错处理中的多处交互问题。
- 修复侧边栏图标、按钮文本、箭头与底部区域的居中和视觉错位问题。
- 修复输入框白边、双层控件和按钮深色残留区域等 Fluent 组件样式问题。

### 清理

- 移除旧 Tauri sidecar 构建脚本与旧 PyInstaller 规格/日志/缓存产物。
- 更新 `.gitignore`，避免误提交 `release/`、`dist/`、PyInstaller 构建目录、运行数据与临时文件。

## [0.2.1] - 2026-03-25

### 清理

- 移除旧的外部 AI 漫画翻译命令链路，统一切换到后端内置图片处理流程。
- 删除设置页中不再使用的漫画 AI 命令、LaMa 相关配置与对应接口。
- 清理 sidecar 构建脚本、PyInstaller 规格文件与仅旧链路使用的依赖项。

### 新增

- 新增漫画工作流，支持将远程链接识别为 `漫画` 类型，并进入按话/按页的导入、下载与阅读流程。
- 新增 `18comic.vip` 与 `bikawebapp.com` 的漫画站点支持。
- 新增 `Grok2API` 翻译提供商配置入口。
- 新增 Bika 账号自动创建与本地凭证复用逻辑。

### 改进

- 阅读进度从“只记录章节”升级为“章节 + 章节内位置”的持久化方案。
- “继续阅读”会尽量恢复到上次退出时的段落或图片附近。
- 切换上下章时自动回到页面顶部开始阅读。
- 切换原文/译文阅读模式时尽量保持当前阅读位置。
- 已安装桌面版默认将数据库、书库、翻译结果与资源写入应用同级 `data/` 目录。

### 修复

- 修复 18comic 阅读阶段的章节图片修复链路，降低图片乱序与切片异常带来的阅读失败概率。
- 修复“继续阅读”恢复时机过早、无法跳到真实位置的问题。
- 修复桌面端非阅读页侧栏跟随页面一起滚动的问题。
- 修复侧栏收缩后 Logo 与折叠按钮重叠的 UI 问题。
- 修复漫画长文本译图溢出、旧版翻译缓存不失效与目标语言识别错误等问题。
